package com.example.urbotanist.mainfragments.search;

import androidx.lifecycle.ViewModel;

public class SearchViewModel extends ViewModel {
  // currently there is no reason to use this viewmodel;
  // sendNewSearchQuery could be put in here, but it interacts with the views of the fragment,
  // so it would make everything more complex to put it here, for no benefit

}